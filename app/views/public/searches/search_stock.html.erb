<!--<link href="https://cdnjs.cloudflare.com/ajax/libs/lightbox2/2.7.1/css/lightbox.css" rel="stylesheet">-->
<!--<script src="https://cdnjs.cloudflare.com/ajax/libs/lightbox2/2.7.1/js/lightbox.min.js" type="text/javascript"></script>-->
<div class='row'>
 <div class="col-md-3 offset-md-1">
   <%= render "public/users/user_info", user: @user %>
 </div>
 <div class="col-md-7">
   <div class="button-wrap">
     <%= link_to "投稿一覧", user_path(user_name: @user.name), class: "radio-button" %>
     <%= link_to "いいね一覧", user_favorites_path(user_name: @user.name), class: "radio-button" %>
     <%= render "public/searches/form_search_stock" %>
   </div>

   <% unless @theme_searched_page.length == 0 %>
     <h2>検索結果: <%= @theme_searched_count %>件</h2>
     <% @theme_searched_page.each do |theme| %>
       <%= render "public/themes/theme_card", theme: theme %>
     <% end %>
     <%= paginate @theme_searched_page, theme: 'bootstrap-5' %>
   <% else %>
     <p>投稿またはいいねした記事があリません</p>
   <% end %>


 </div>
</div>