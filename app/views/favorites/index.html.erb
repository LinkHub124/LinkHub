<p><h1><%= @user.name %>のFavorite一覧ページ</h1></p>
<p><%= link_to "投稿一覧ページへ" , user_path(user_name: @user.name) %></p>

<% unless current_user == @user %>
  <% if current_user.following?(@user) %>
    <%= link_to "フォロー外す", user_relationships_path(user_name: @user.name), method: :delete, class: "btn btn-danger" %>
  <% else %>
    <%= link_to "フォローする", user_relationships_path(user_name: @user.name), method: :post, class: "btn btn-primary" %>
  <% end %>
<% end %>


フォロー数: <%= @user.followings.count %></td>
      <td>フォロワー数: <%= @user.followers.count %></td>
    </tr>
  </tbody>
</table>

<table>
  <thead>
    <tr>
      <th>Title</th>
      <th colspan="2"></th>
    </tr>
  </thead>
  <tbody>
    <% @user.favorites.each do |favorite| %>
    <tr>
      <td><%= favorite.theme.title %></td>
      <td><%= link_to "Show", theme_path(user_name: favorite.theme.user.name, theme_hashid: favorite.theme.hashid) %></td>
      <td><%= link_to "Edit", edit_theme_path(user_name: favorite.theme.user.name, theme_hashid: favorite.theme.hashid) %></td>
    </tr>
    <% end %>
  </tbody>
</table>



